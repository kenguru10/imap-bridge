{
  "name": "cloud-mail-imap-bridge",
  "version": "1.0.0",
  "description": "IMAP/SMTP bridge between Cloud Mail API and local Maildir + Dovecot",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "mailparser": "^3.7.0",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.0",
    "smtp-server": "^3.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
